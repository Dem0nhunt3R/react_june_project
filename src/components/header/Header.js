import {UserBar} from "../userBar/UserBar";
import {NavBar} from "../navBar/NavBar";

const Header = () => {
    return (
        <div>
            <UserBar/>
            <NavBar/>
        </div>
    );
};

export {Header};